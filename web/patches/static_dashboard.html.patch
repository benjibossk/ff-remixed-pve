diff --git a/static/dashboard.html b/static/dashboard.html
index 408c029..ef83634 100644
--- a/static/dashboard.html
+++ b/static/dashboard.html
@@ -3,7 +3,7 @@
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>Dashboard - [GTG] Live Map</title>
+    <title>PC — Gestion des Secteurs</title>
     <link href="/api/v1/cdn/map/bootstrap.min.css" rel="stylesheet">
     <link rel="stylesheet" href="/api/v1/cdn/map/icons/bootstrap-icons.min.css">
     <style>
@@ -21,20 +21,33 @@
         .avatar-sm { width: 24px; height: 24px; }
         .dropdown-menu { background-color: rgba(30, 35, 41, 0.9); backdrop-filter: blur(10px); border: 1px solid rgba(255, 255, 255, 0.1); }
     </style>
+    <link rel="preconnect" href="https://fonts.googleapis.com">
+    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
+    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@300;400;500;600;700&family=IBM+Plex+Mono:wght@400;500;600&display=swap" rel="stylesheet">
+    <link rel="stylesheet" href="/static/theme.css">
 </head>
 <body>
+<div class="tricolore"></div>
 
 <nav class="navbar navbar-expand-lg navbar-dark sticky-top glass-nav">
     <div class="container">
-        <a class="navbar-brand d-flex align-items-center" href="/"><i class="bi bi-broadcast-pin me-2 fs-4"></i><span class="fw-bold">[GTG] Live Map</span></a>
+        <a class="navbar-brand d-flex align-items-center" href="/"><span class="crest"><svg viewBox="0 0 24 24" fill="none" stroke="#3d8bff" stroke-width="1.5" stroke-linejoin="round"><path d="M12 2 L20 6 V12 C20 17 16.2 20.6 12 22 C7.8 20.6 4 17 4 12 V6 Z"/><path d="M12 7 V15 M8.4 11 H15.6" stroke="#39c2d1"/></svg></span><span class="fw-bold">PC — Gestion des Secteurs</span></a>
         <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"><span class="navbar-toggler-icon"></span></button>
         <div class="collapse navbar-collapse" id="navbarNav"><ul class="navbar-nav ms-auto align-items-center"><li id="user-display" class="nav-item ms-lg-2 mt-2 mt-lg-0"></li></ul></div>
     </div>
 </nav>
 
 <div class="container-lg mt-4" id="dashboard-container">
-    <div class="d-flex justify-content-between align-items-center mb-4">
-        <h1 class="display-5 fw-bold text-white">Dashboard</h1>
+    <div class="d-flex justify-content-between align-items-end mb-4 flex-wrap gap-2">
+        <div>
+            <div class="classif d-inline-block mb-2">Diffusion Restreinte</div>
+            <h1 class="display-5 fw-bold text-white mb-0">Gestion des Secteurs</h1>
+            <div class="text-muted mt-1" style="letter-spacing:.14em;text-transform:uppercase;font-size:.8rem">Poste de commandement · Serveurs & théâtres</div>
+        </div>
+        <div class="d-flex gap-2">
+            <button id="link-char-btn" class="btn btn-warning"><i class="bi bi-link-45deg me-1"></i> Lier mon perso</button>
+            <a href="/map.html" class="btn btn-info"><i class="bi bi-map-fill me-1"></i> Ouvrir la carte</a>
+        </div>
     </div>
 
     <div id="dashboard-content">
@@ -48,24 +61,24 @@
     <div class="modal-dialog">
         <div class="modal-content dashboard-card">
             <div class="modal-header">
-                <h1 class="modal-title fs-5">Add New Server</h1>
+                <h1 class="modal-title fs-5">Ajouter un serveur</h1>
                 <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
             </div>
             <div class="modal-body">
                 <form id="add-server-form">
                     <div class="mb-3">
-                        <label for="serverNameInput" class="form-label">Server Name</label>
+                        <label for="serverNameInput" class="form-label">Nom du serveur</label>
                         <input type="text" class="form-control" id="serverNameInput" required>
                     </div>
                     <div class="mb-3">
-                        <label for="mapConfigSelect" class="form-label">Map</label>
+                        <label for="mapConfigSelect" class="form-label">Théâtre / Carte</label>
                         <select class="form-select" id="mapConfigSelect" required>
-                            <option selected disabled value="">Please select a map...</option>
+                            <option selected disabled value="">Sélectionner une carte...</option>
                         </select>
                     </div>
                     <div class="modal-footer">
-                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
-                        <button type="submit" class="btn btn-primary">Create Server</button>
+                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
+                        <button type="submit" class="btn btn-primary">Créer le serveur</button>
                     </div>
                 </form>
             </div>
@@ -78,15 +91,15 @@
     <div class="modal-dialog modal-lg">
         <div class="modal-content dashboard-card">
             <div class="modal-header">
-                <h1 class="modal-title fs-5">Edit Server</h1>
+                <h1 class="modal-title fs-5">Gérer le serveur</h1>
                 <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
             </div>
             <div class="modal-body">
                 <nav>
                     <div class="nav nav-tabs" id="nav-tab" role="tablist">
-                        <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#nav-general">General</button>
-                        <button class="nav-link" data-bs-toggle="tab" data-bs-target="#nav-logsource">Log Source</button>
-                        <button class="nav-link" data-bs-toggle="tab" data-bs-target="#nav-access">Access Control</button>
+                        <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#nav-general">Général</button>
+                        <button class="nav-link" data-bs-toggle="tab" data-bs-target="#nav-logsource">Source des logs</button>
+                        <button class="nav-link" data-bs-toggle="tab" data-bs-target="#nav-access">Accès</button>
                     </div>
                 </nav>
                 <div class="tab-content pt-3" id="nav-tabContent">
@@ -161,10 +174,10 @@
                 </div>
             </div>
             <div class="modal-footer d-flex justify-content-between">
-                <button type="button" class="btn btn-warning" id="regenerate-key-btn">Regenerate API Key</button>
+                <button type="button" class="btn btn-warning" id="regenerate-key-btn">Régénérer la clé API</button>
                 <div>
-                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
-                    <button type="button" id="save-all-changes-btn" class="btn btn-primary">Save All Changes</button>
+                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fermer</button>
+                    <button type="button" id="save-all-changes-btn" class="btn btn-primary">Enregistrer</button>
                 </div>
             </div>
         </div>
@@ -176,7 +189,7 @@
     <div class="modal-dialog modal-xl">
         <div class="modal-content dashboard-card">
             <div class="modal-header">
-                <h1 class="modal-title fs-5">Server Setup Guide: <span id="wizard-server-name"></span></h1>
+                <h1 class="modal-title fs-5">Assistant de configuration : <span id="wizard-server-name"></span></h1>
                 <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
             </div>
             <div class="modal-body">
@@ -253,6 +266,23 @@
     </div>
 </div>
 
+<!-- Lier mon perso en jeu (code de liaison) -->
+<div class="modal fade" id="linkCharModal" tabindex="-1">
+    <div class="modal-dialog modal-dialog-centered">
+        <div class="modal-content dashboard-card">
+            <div class="modal-header"><h5 class="modal-title">Lier mon perso en jeu</h5>
+                <button class="btn-close" data-bs-dismiss="modal"></button>
+            </div>
+            <div class="modal-body text-center">
+                <p class="text-muted mb-1">Rejoins le serveur en jeu et tape dans le chat :</p>
+                <div class="my-3"><code style="font-size:1.1rem">#link <span id="link-code-cmd" style="font-size:1.6rem;letter-spacing:.2em">------</span></code></div>
+                <p class="small text-muted">Le code expire dans <span id="link-code-ttl">10</span> min · usage unique.</p>
+                <p class="small">Une fois lié, tu pourras déplacer/supprimer <strong>tes</strong> marqueurs depuis le site.</p>
+            </div>
+        </div>
+    </div>
+</div>
+
 <script src="/api/v1/cdn/map/bootstrap.bundle.min.js"></script>
 <script>
     document.addEventListener('DOMContentLoaded', () => {
@@ -272,7 +302,7 @@
         const editIsListedSwitch = document.getElementById('editIsListedSwitch');
         const isListedContainer = document.getElementById('is-listed-container');
 
-        const updateUserDisplay = (user) => { const d = document.getElementById('user-display'); if (user && user.id) { d.innerHTML = `<div class="dropdown"><a href="#" class="d-block link-light text-decoration-none dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false"><img src="${user.avatar}" alt="${user.username}" width="32" height="32" class="rounded-circle"><span class="ms-2 d-none d-md-inline">${user.username}</span></a><ul class="dropdown-menu dropdown-menu-dark text-small"><li><a class="dropdown-item active" href="/dashboard.html">Dashboard</a></li><li><a class="dropdown-item" href="/map.html">Live Map</a></li><li><hr class="dropdown-divider"></li><li><a class="dropdown-item" href="/auth/logout">Logout</a></li></ul></div>`; } else { window.location.href = '/auth/discord/login'; } };
+        const updateUserDisplay = (user) => { const d = document.getElementById('user-display'); if (user && user.id) { d.innerHTML = `<div class="dropdown"><a href="#" class="d-block link-light text-decoration-none dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false"><img src="${user.avatar}" alt="${user.username}" width="32" height="32" class="rounded-circle"><span class="ms-2 d-none d-md-inline">${user.username}</span></a><ul class="dropdown-menu dropdown-menu-dark text-small"><li><a class="dropdown-item active" href="/dashboard.html">Dashboard</a></li><li><a class="dropdown-item" href="/map.html">Live Map</a></li>${user.account_type === 'admin' ? '<li><a class="dropdown-item" href="/admin.html"><i class="bi bi-shield-lock me-1"></i>Admin</a></li>' : ''}<li><hr class="dropdown-divider"></li><li><a class="dropdown-item" href="/auth/logout">Logout</a></li></ul></div>`; } else { window.location.href = '/auth/discord/login'; } };
         async function fetchData(ep, opt={}) { try { const res = await fetch(`${API_BASE_URL}/${ep}`, opt); const data = await res.json(); if (!res.ok) { throw new Error(data.error||data.message||`HTTP error! ${res.status}`); } return data; } catch (e) { console.error(`API Error for ${ep}:`, e.message); alert(`An error occurred: ${e.message}`); throw e; } }
 
         function calculateServerStatus(statusData) {
@@ -292,31 +322,33 @@
         function renderDashboard() {
             let html = '';
 
-            const renderGroup = (title, servers, isShared = false) => {
+            const renderGroup = (title, servers, isShared = false, icon = 'bi bi-hdd-network-fill') => {
                 if (!servers || servers.length === 0) return '';
 
                 let listHtml = '';
                 servers.forEach((server) => {
                     const status = calculateServerStatus(userServersStatus[server.id]);
 
+                    // Mode serveur unique : plus de bouton « Supprimer » (il n'y a qu'un
+                    // serveur, l'effacer viderait le site) et plus de lien de partage
+                    // avec ?server_id= (la carte resout le serveur toute seule).
                     let buttonsHtml = `
-                        <button class="btn btn-sm btn-outline-warning setup-guide-btn" data-server-id="${server.id}" title="Setup Guide"><i class="bi bi-gear-fill"></i> Setup</button>
-                        <button class="btn btn-sm btn-info share-btn" data-link="/map.html?server_id=${server.id}" title="Copy map link"><i class="bi bi-share-fill"></i></button>
-                        <button class="btn btn-sm btn-secondary manage-btn" data-server-id="${server.id}">Manage</button>
-                        <button class="btn btn-sm btn-danger delete-btn" data-server-id="${server.id}" data-server-name="${server.name}">Delete</button>
+                        <button class="btn btn-sm btn-warning setup-guide-btn" data-server-id="${server.id}" title="Assistant de configuration"><i class="bi bi-gear-fill"></i> Config</button>
+                        <a href="/map.html" class="btn btn-sm btn-info" title="Ouvrir la carte"><i class="bi bi-map-fill"></i></a>
+                        <button class="btn btn-sm btn-secondary manage-btn" data-server-id="${server.id}">Gérer</button>
                     `;
 
                     if (isShared) {
-                        buttonsHtml = `<a href="/map.html?server_id=${server.id}" class="btn btn-sm btn-info"><i class="bi bi-map-fill"></i> View Map</a>`;
+                        buttonsHtml = `<a href="/map.html" class="btn btn-sm btn-info"><i class="bi bi-map-fill"></i> Voir la carte</a>`;
                     }
 
                     listHtml += `
                         <div class="list-group-item bg-transparent text-white d-flex flex-wrap justify-content-between align-items-center gap-2">
                             <div class="d-flex align-items-center">
-                                <i id="status-dot-${server.id}" class="bi bi-circle-fill fs-5 me-3 text-secondary" title="Loading status..."></i>
+                                <i id="status-dot-${server.id}" class="bi bi-circle-fill fs-5 me-3 text-secondary" title="Chargement du statut..."></i>
                                 <div>
                                     <strong>${server.name}</strong><br>
-                                    <small class="text-muted">Map: ${server.map_config.Name}</small>
+                                    <small class="text-muted">Théâtre : ${server.map_config.Name}</small>
                                 </div>
                             </div>
                             <div class="btn-group" role="group">${buttonsHtml}</div>
@@ -324,22 +356,29 @@
                 });
 
                 return `
-                    <div class="card dashboard-card mb-4">
-                        <div class="card-header d-flex justify-content-between align-items-center">${title}</div>
+                    <div class="card dashboard-card mb-4 bracket">
+                        <div class="card-header d-flex justify-content-between align-items-center"><span><i class="${icon} me-2"></i>${title}</span></div>
                         <div class="list-group list-group-flush">${listHtml}</div>
                     </div>`;
             };
 
-            html += renderGroup('My Servers', userServers.owned);
-            html += renderGroup('Shared With Me', userServers.shared, true);
-            html += renderGroup('Admin View (All Other Servers)', userServers.admin);
+            // Mode serveur unique : les trois groupes (« Mes Serveurs » / « Partagés avec
+            // moi » / « Vue Admin ») n'ont plus de sens pour un seul serveur. On les
+            // fusionne en une seule entrée, en dedupliquant par id car le meme serveur
+            // peut apparaitre dans plusieurs listes (proprietaire ET admin).
+            const seenIds = new Set();
+            const theServer = []
+                .concat(userServers.owned || [], userServers.shared || [], userServers.admin || [])
+                .filter(s => s && !seenIds.has(s.id) && seenIds.add(s.id));
+
+            html += renderGroup('Serveur', theServer, false, 'bi bi-hdd-network-fill');
 
             if (html === '') {
-                html = `<div class="card dashboard-card"><div class="card-body text-center p-4">You have not added or been assigned to any servers yet.</div></div>`;
+                html = `<div class="card dashboard-card bracket"><div class="card-body text-center p-5"><i class="bi bi-hdd-stack fs-1 text-muted d-block mb-3"></i>Aucun serveur configuré.<br><small class="text-muted">Contactez un administrateur.</small></div></div>`;
             }
 
-            const addButton = `<div class="d-flex justify-content-end mb-3"><button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#addServerModal">+ Add New Server</button></div>`;
-            dashboardContent.innerHTML = addButton + html;
+            // Plus de bouton « Ajouter un serveur » : l'instance est mono-serveur.
+            dashboardContent.innerHTML = html;
             attachActionListeners();
         }
 
@@ -674,6 +713,15 @@
         userSearchInput.addEventListener('input', handleUserSearch);
         document.body.addEventListener('click', (e) => { if (!e.target.closest('#userSearchInput')) userSearchResults.classList.add('d-none'); });
 
+        document.getElementById('link-char-btn').addEventListener('click', async () => {
+            try {
+                const d = await fetchData('link/code', { method: 'POST' });
+                document.getElementById('link-code-cmd').textContent = d.code;
+                document.getElementById('link-code-ttl').textContent = Math.round((d.expires_in || 600) / 60);
+                new bootstrap.Modal(document.getElementById('linkCharModal')).show();
+            } catch (e) {}
+        });
+
         initializeDashboard();
     });
 </script>
